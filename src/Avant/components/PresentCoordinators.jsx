import React from 'react';
import Grid from '../../components/Grid';

function PresentCoordinators(props) {
    return (
        <Grid
            heading='Present Coordinators'
        >
        </Grid>
    );
}

export default PresentCoordinators;