import React from 'react';
import Grid from '../../components/Grid';

function Events(props) {
    return (
        <Grid
            heading='Events'
        >
        </Grid>
    );
}

export default Events;