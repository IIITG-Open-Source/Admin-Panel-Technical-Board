import React from 'react';
import Grid from '../../components/Grid';

function PastCoordinators(props) {
    return (
        <Grid
            heading='Past Coordinators'
        >
        </Grid>
    );
}

export default PastCoordinators;