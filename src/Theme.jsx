import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#9c27b0',
        },
        secondary: {
            light: '#f73378',
            main: '#f50057',
        },
    },
});

export default theme;